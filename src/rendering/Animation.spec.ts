import { Animation } from './Animation'

describe('Animation', () => {
  describe('Frame callbacks', () => {
    it.todo('calls all callbacks on the correct frames')

    it.todo('safely ignores out-of-range indices')
  })
})
